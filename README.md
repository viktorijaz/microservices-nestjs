## Running the app

```bash
# development
$ docker-compose up

## Microservices and NestJs

NestJs makes for an easy build of a monorepo apps, with shared / common libraries and built-in protocol layers such as TCP-only, or using RabbitMQ or Kafka on top of it for communicating between different services.
